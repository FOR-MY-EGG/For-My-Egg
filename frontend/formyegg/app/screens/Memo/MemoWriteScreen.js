import React from 'react';
import {Text, View} from 'react-native';

const MemoWriteScreen = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>MemoWriteScreen! 🎉</Text>
    </View>
  );
};

export default MemoWriteScreen;
